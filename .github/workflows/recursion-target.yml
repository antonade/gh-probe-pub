name: recursion-target
on:
  workflow_dispatch:
  repository_dispatch:
    types: [probe]
  push:
    branches: [rectarget]
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TARGET_RAN event=${{ github.event_name }} actor=${{ github.actor }}"
