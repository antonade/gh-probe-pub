name: reusable-caller
on: { push: { branches: [reuseprobe] } }
jobs:
  narrow:
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-writer.yml
  wide:
    permissions:
      contents: read
      issues: write
    uses: ./.github/workflows/reusable-writer.yml
